# GENERATED by DeepSeek via OpenRouter, review before trusting.
# Regression: Koffein-Eintrag persistiert nach App-Neustart (relaunch_persistence.yaml)
appId: com.tobiaskoch.NutritionApp
---
- launchApp:
    stopApp: true
- waitForAnimationToEnd
- extendedWaitUntil:
    visible: "Trinken"
    timeout: 30000
- tapOn:
    point: "198,822"
- waitForAnimationToEnd
# Beweis fuer aktiven Trinken-Tab: echtes Scrollen zum obersten Wasser-Button
# (IDs inaktiver Tabs stehen in der Hierarchie, nur Scroll/Sicht beweist den Screen).
- scrollUntilVisible:
    element:
      id: "fluids.water.add250"
    direction: UP
# Pre-Clean: bestehende Eintraege loeschen
- scrollUntilVisible:
    element:
      id: "fluids.entry.delete"
    direction: DOWN
    timeout: 10000
    optional: true
- repeat:
    times: 8
    while:
      visible:
        id: "fluids.entry.delete"
    commands:
      - swipe:
          direction: UP
          duration: 300
      - waitForAnimationToEnd
      - tapOn:
          id: "fluids.entry.delete"
          index: 0
      - waitForAnimationToEnd
# Zurueck nach oben zu den Quick-Add-Buttons
- scrollUntilVisible:
    element:
      id: "fluids.drink.Espresso"
    direction: UP
# Espresso loggen (erzeugt Koffein- und Wasser-Eintrag)
- tapOn:
    id: "fluids.drink.Espresso"
- waitForAnimationToEnd
# Verifizieren dass Eintraege angezeigt werden
- scrollUntilVisible:
    element:
      id: "fluids.entry.caffeine"
    direction: DOWN
- assertVisible:
    id: "fluids.entry.caffeine"
# App stoppen und neu starten (stopApp: true, NIEMALS clearState)
- launchApp:
    stopApp: true
- waitForAnimationToEnd
- extendedWaitUntil:
    visible: "Trinken"
    timeout: 30000
- tapOn:
    point: "198,822"
- waitForAnimationToEnd
# Beweis fuer aktiven Trinken-Tab: echtes Scrollen zum obersten Wasser-Button
# (IDs inaktiver Tabs stehen in der Hierarchie, nur Scroll/Sicht beweist den Screen).
- scrollUntilVisible:
    element:
      id: "fluids.water.add250"
    direction: UP
# Nach Neustart: Koffein-Eintrag muss noch vorhanden sein (Persistenz-Test)
- scrollUntilVisible:
    element:
      id: "fluids.entry.caffeine"
    direction: DOWN
- assertVisible:
    id: "fluids.entry.caffeine"
# Aufraeumen: beide Eintraege loeschen (Wasser + Koffein)
- swipe:
    direction: UP
    duration: 300
- waitForAnimationToEnd
- tapOn:
    id: "fluids.entry.delete"
    index: 0
- waitForAnimationToEnd
- runFlow:
    when:
      visible:
        id: "fluids.entry.delete"
    commands:
      - swipe:
          direction: UP
          duration: 300
      - waitForAnimationToEnd
      - tapOn:
          id: "fluids.entry.delete"
          index: 0
      - waitForAnimationToEnd
- assertNotVisible:
    id: "fluids.entry.caffeine"
